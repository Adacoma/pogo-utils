# Reuse the current Vicsek integration build without copying its SDK/Pogosim
# rules. MAKEFILE_DIR selects this example's source and output target name.
MAKEFILE_DIR := acu
include ../vicsek/Makefile
